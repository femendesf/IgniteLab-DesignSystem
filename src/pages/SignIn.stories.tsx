import { Meta, StoryObj } from '@storybook/react'
import { Signin} from './SignIn'

export default{
    title: 'Pages/Sign in',
    component: SignIn,
    args:{},
    
} as Meta

export const Default: StoryObj = {}
